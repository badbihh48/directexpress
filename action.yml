name: 
description: 
author: 

inputs:
  prompt:
    description: 
    required: 
    default:
  GEMINI_API_KEY:
    description: 
    required: 
  OTLP_GCP_WIF_PROVIDER:
    description: 
    required: 
  OTLP_GOOGLE_CLOUD_PROJECT:
    description: 
    required: 
  settings_json:
    description: 
       include:
      telemetry:  
    required: 
  version:
    description: 
    required: 
    default: 
outputs:
  summary:
    description: 
    value: 
runs:
  using: 
  steps:
     name: 
      if: 
      run: 
      shell: 
      env:
        SETTINGS_JSON: 

     name:  
      if: 
      id: 
      uses: 
      with:
        project_id: 
        workload_identity_provider:

     name:
      if: 
      run: 
      shell: 
      env:
        OTLP_GOOGLE_CLOUD_PROJECT: 

     name: 
      id: 
      run: 
      
          echo: 
          echo: 
        
        Verifying installation:
          echo Error: 
        
      shell:

     name: 
      id: 
      run: 
    
      shell: 
      env:
        GEMINI_API_KEY: 
        PROMPT: 
        SURFACE: 

branding:
  icon: 
  color: 
